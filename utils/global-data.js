export const getGlobalData = () => {
  const name = 'LLMS are Going Great!';
  const blogTitle = 'LLMS are Going Great!';
  const footerText = '';

  return {
    name,
    blogTitle,
    footerText,
  };
};
